<div class="col" style="width:50%; float:left;">
<table class="goal-table">
<caption>
    Входящие задания
  </caption>
  <tr> <!--ряд с ячейками заголовков-->
    <th>тема</th>
    <th>задание</th>
    <th>от кого</th>
    <th>срок исполнения</th>
    <th>статус</th>
    <th></th>
  </tr> 
  <% @tasks.each do |task| %>
    <tr> <!--ряд с ячейками тела таблицы-->
     <td><%= task.goal.topic%></td>
     <td><%= task.goal.description%></td>
     <%author = User.find_by(id: task.goal.user_id)%>
      <td><%= author.surname%> <%= author.name[0]%>.<%= author.patronymic[0]%>.</td>
     <td>18.05.2022 14:00</td>
     <td>В работе</td>
     <td><%= link_to 'открыть', goal_task_path(task.goal.id, task), style: 'color:#5C2018;' %></td>
    </tr> 
  <% end %>
 </table>
</div>
<div class="col" style="float:left; padding-left:60px;">
<table class="route-table">
<caption>
    Участие в маршрутах
  </caption>
  <tr> <!--ряд с ячейками заголовков-->
    <th>маршрут</th>
    <th>этап</th>
    <th></th>
  </tr> 
  <% @steps.each do |step| %>
    <tr> <!--ряд с ячейками тела таблицы-->
     <td><%#= step.route.route_name%></td>
     <td><%= step.step_type%></td>
     <% #route = step.route_id%>
     <td><%#= link_to 'открыть', route_step_path(route, step), style: 'color:#5C2018;' %></td>
    </tr> 
  <% end %>
 </table>
</div>